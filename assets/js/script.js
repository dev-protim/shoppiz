jQuery(function ($) {

    'use strict';

    // Hero carousel starts
    $('.hero-carousel').owlCarousel({
        loop: true,
        margin: 0,
        nav: false,
        dots: true,
        items: 1,
        autoplay: true,
        smartSpeed: 500
    })
    // Hero carousel ends
});